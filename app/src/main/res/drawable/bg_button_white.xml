<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base -->
    <solid android:color="#FFFFFF"/>

    <!-- Smooth rounded -->
    <corners android:radius="30dp"/>

    <!-- Subtle border (adds depth) -->
    <stroke
        android:width="1dp"
        android:color="#E2E8F0"/>

</shape>